# ── Claude API (all models use the same key) ──
# Required for analysis (sba analyze) AND the Producer Copilot chatbot
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Voyage (for RAG embeddings) ──
# Required only for full RAG mode (sba index-corpus / sba analyze --use-rag)
VOYAGE_API_KEY=your-voyage-key-here

# ── ElevenLabs (voice output) ──
# Required for voice mode only
ELEVENLABS_API_KEY=your-elevenlabs-key-here
ELEVENLABS_VOICE_ID=your-voice-id-here

# ── OpenAI (Whisper STT only) ──
# Required for voice input only
OPENAI_API_KEY=sk-your-openai-key-here

# ── Budget ──
# Path to the master budget Excel file
MASTER_BUDGET_PATH=./NPA_MasterBudget_v1.xlsx
